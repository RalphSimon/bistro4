<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{% block title %}Mixture + Compass{% endblock %}</title>
        {{ "your-project.css" | asset_url | stylesheet_tag }}
        
    </head>
    <body class="wrapper">
        {% include "navigation" %}
        <div class="container">
            {% block content %}
            
            {% endblock %}
        </div>
    </body>
</html>